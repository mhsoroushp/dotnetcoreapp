# dotnetcoreapp

ssh-keygen -t ed25519 -C "sorosh.hadi@gmail.com"

copy the .pub key in paste in the github ssh-key in setting. 

To check if you have now the permission ``ssh -T git@github.com` you will get "hi username"
